import * as add from "./add";
import * as create from "./create";
import * as remove from "./remove";
import * as show from "./show";

export const commands = [add, create, remove, show];

export const name = "invite";
