import * as add from "./add";
import * as get from "./get";
import * as remove from "./remove";
import * as set from "./set";

export const subcommands = [add, get, remove, set];

export const commands = [];

export const name = "settings";
